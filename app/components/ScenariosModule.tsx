import { Card, CardHeader, Alert } from "./ui";

export default function ScenariosModule() {
  return (
    <Card>
      <CardHeader title="Training Scenarios" subtitle="Certification Fail-States" />
      <div className="mt-6">
        <Alert variant="info">
          Training scenarios module - Full implementation in progress.
        </Alert>
      </div>
    </Card>
  );
}
